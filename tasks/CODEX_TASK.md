# Codex work item

Implement the provider-neutral CLI and one-job PostgreSQL runner against
`contracts/adapter-contract.json`. Work contract-first and test failure paths
before implementation. Do not implement `DONE`, deployment, budget grants, or
permission changes. Return code, tests, exact commands, exit codes, and a list
of unverified assumptions.

This work item is `BLOCKED` until a dedicated PostgreSQL test service is
available. Planning and test fixtures may proceed without inventing a passing
database run.
