# pi work item

Independently review the frozen adapter contract and implement the pi adapter
without reading Codex's implementation decisions beyond the canonical
contract. Prove discovery, claim, heartbeat, checkpoint, submit-for-review,
failure, cancellation, stale revision, stale fencing, and reconciliation
behavior using the shared contract tests.

Do not run until the real pi executable and its identity/version are available.
Never simulate a pi run or reuse Codex output as independent evidence.
