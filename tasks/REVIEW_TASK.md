# Independent review work item

Compare both real adapter runs against the same frozen cases. Recompute hashes,
replay canonical events, challenge every completion claim, and run adversarial
probes. Verify that no agent can write `DONE`, spend money, expand permissions,
or bypass reconciliation. Return `PASS`, `REVISE`, or `BLOCKED` with evidence;
never infer PASS from missing data.
